# PayFlow Demo

A small payment-processing demo service.

## Modules

- `app/models.py` — customer and card data models
- `app/payments.py` — charge processing

## Setup

```bash
pip install -r requirements.txt
python -m pytest tests/ -q
```
